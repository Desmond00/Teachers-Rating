select * from STUDENTS_ROLLNUMBERS where roll_number = '100200300';

desc STUDENTS_ROLLNUMBERS;

create table Admin (id varchar2(10), password varchar2(10));

insert into Admin values('admin','12345');